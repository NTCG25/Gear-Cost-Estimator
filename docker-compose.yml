services:
  db:
    container_name: postgres
    image: postgres:alpine
    environment:
      POSTGRES_USER: nigelm
      POSTGRES_PASSWORD: T00quicksli3ktri3k
      PGDATA: /data/postgres
      volumes:
        - db:/data/postgres
      ports:
        - "5432:5432"
      networks:
        - db
      restart: unless-stopped

  networks:
    db:
      driver: bridge

  volumes:
    db:
